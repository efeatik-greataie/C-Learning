#include <stdio.h>
#include <string.h>
#include <ctype.h> 

typedef char * string;

int main(int argc, char *argv[]) {
// a program that inspects the user inputted string and lets user to see and make changes. 
// There will be an inspector that how much a,b,c,d...z has beed used or how many uppercases are included etc. 

        int x = 42;
        int *p = &x;

        printf("%p", p);
}




